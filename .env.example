# Lấy key ở https://aistudio.google.com/apikey -> Create API key (miễn phí, không cần thẻ)
GEMINI_API_KEY=AIza...

# Tuỳ chọn. Bỏ trống là dùng gemini-flash-latest (luôn trỏ tới bản Flash mới nhất).
# Muốn cố định một model: gemini-3.6-flash
# Muốn hạn mức miễn phí cao hơn: gemini-flash-lite-latest
MAM_MODEL=
